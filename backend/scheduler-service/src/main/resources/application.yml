server:
  port: 8086

spring:
  application:
    name: scheduler-service
  datasource:
    url: jdbc:mysql://mysql-master:3306/yinling_main?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Yinling@2025
  redis:
    host: redis
    port: 6379
    password: Yinling@2025
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: admin
    password: Yinling@2025
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848

logging:
  level:
    root: INFO
    com.yinling: DEBUG

# 定时任务配置
scheduling:
  # 用药提醒检查 (每小时)
  medication-reminder: 0 0 * * * ?
  # 健康报告生成 (每天凌晨3点)
  health-report: 0 0 3 * * ?
  # 数据备份 (每天凌晨2点)
  data-backup: 0 0 2 * * ?
